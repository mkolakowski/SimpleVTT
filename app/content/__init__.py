"""Content-layer helpers — parsers, lookups, and projections from the
shipped SRD / homebrew JSON content into the shapes the route handlers
consume.

v2.49.74 — first member is ``range_parser`` (Phase 2B of the
ruler/range plan; surfaces a single ``parse_range_ft`` function for
the cast / attack endpoints' range-enforcement checks).
"""
