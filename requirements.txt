fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
jinja2==3.1.4
python-multipart==0.0.12
itsdangerous==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
authlib==1.3.2
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
websockets==13.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0
mutagen==1.47.0
Pillow==11.2.1
markdown==3.7
# v2.1042.0 — HTML sanitizer (Rust/ammonia bindings) for the wiki markdown→HTML
# render path, so raw HTML embedded in a doc can never become stored XSS.
nh3==0.3.6
# v2.985.0 — headless Chromium (Playwright) powers the Admin-Center demo
# self-test's optional video capture (it records each campaign's tabletop while
# the run drives it). The browser binary is installed separately in the
# Dockerfile (playwright install chromium). Same version as requirements-dev.
playwright==1.47.0
